#ifndef HELPER_H
#define HELPER_H

#include <string>

bool string_bool( std::string text);
bool file_exist( std::string file);

class helper
{
    public:
        helper();
        virtual ~helper();

    protected:

    private:
};

#endif // HELPER_H
