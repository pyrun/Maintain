#ifndef HELPER_H
#define HELPER_H

#include <string>

bool string_bool( std::string text);

class helper
{
    public:
        helper();
        virtual ~helper();

    protected:

    private:
};

#endif // HELPER_H
